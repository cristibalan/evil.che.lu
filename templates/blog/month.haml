---
# don't edit this file, it's automatically generated from the template each time
title:      <%= title %>
created_at: <%= Time.now.to_y %>
filter:
  - erb
  - haml
dirty:      true
layout: default
---
%h1= h(@page.title)

- articles = @pages.find(:all, :in_directory => @page.dir, :recursive => true, :sort_by => "created_at", :reverse => true, :blog_post => true)
- articles.delete(@page)

- if articles.empty?
  %p No articles found.
- else
  %ul.list
    - articles.each do |page|
      %li
        = link_to_page(page)
        %em== (#{page.created_at.strftime('%Y-%m-%d')})
